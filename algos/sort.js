const bubbleSort = (arr) => {
  return [];
};

const selectionSort = (arr) => {
  return [];
};

const insertionSort = (arr) => {
  return [];
};

const mergeSort = (arr) => {
  return [];
};

const quickSort = (arr) => {
  return [];
};

const radixSort = (arr) => {
  return [];
};


module.exports = {
  bubbleSort,
  selectionSort,
  insertionSort,
  mergeSort,
  quickSort,
  radixSort,
};