export const bubbleSort = (arr: Array<number>): Array<number> => {
  return [];
};

export const selectionSort = (arr: Array<number>): Array<number> => {
  return [];
};

export const insertionSort = (arr: Array<number>): Array<number> => {
  return [];
};

export const mergeSort = (arr: Array<number>): Array<number> => {
  return [];
};

export const quickSort = (arr: Array<number>): Array<number> => {
  return [];
};

export const radixSort = (arr: Array<number>): Array<number> => {
  return [];
};

/*
module.exports = {
  bubbleSort,
  selectionSort,
  insertionSort,
  mergeSort,
  quickSort,
  radixSort,
}
*/